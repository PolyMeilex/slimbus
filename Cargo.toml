[workspace]
members = ["slimbus"]

[package]
name = "examples"
edition = "2021"

[dev-dependencies]
slimbus = { path = "./slimbus" }
calloop = "0.12"

serde = { version = "1", features = ["derive"] }
rand = "0.8.5"
zvariant = { version = "4.0.0", default-features = false }
